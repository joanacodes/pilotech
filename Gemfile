source "https://rubygems.org"

# Même environnement que GitHub Pages (Jekyll 3.10, jekyll-feed, kramdown GFM…)
gem "github-pages", group: :jekyll_plugins
gem "webrick"

platforms :mingw, :x64_mingw, :mswin do
  gem "tzinfo-data"
end
